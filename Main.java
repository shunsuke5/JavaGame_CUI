public class Main {
    public static void main(String[] args) {
        /*
        
        // 主人公を生み出す
        Brave b = new Brave();
        // 冒険の始まり
        Event.startEvent();
        // マップ選択
        Event.chooseMap();
        // マップ内アクション
        Event.chooseMapAction();
        // バトル
        Event.battle();
        
        */

        /*
         * b.attack();
         * e.attack();
         * b.spell();
         * e.attack();
         * b.item();
         * e.attack();
         * b.attack();
         * 
         * battle end
         * 
         * if (b.agility > e.agility) {
         *      b.turn;     // 先攻
         *      e.turn;     // 後攻
         * } else {
         *      e.turn;     // 先攻
         *      b.turn;     // 後攻
         * }
         */
    }
}